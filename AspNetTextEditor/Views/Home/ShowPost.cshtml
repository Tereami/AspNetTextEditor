@model TextEditorViewModel
@{
    ViewData["Title"] = Model.Title;
}

<p>
    <a asp-action="Index">
        @(
            "<< "
        )
        Назад к списку
    </a>
</p>
<h2>@Model.Title</h2>

<div>   
   <rich-text text="@Model.Message" />
</div>

<hr />
<p>
    <a asp-action="Edit" asp-route-id="@Model.Id">Редактировать</a>
</p>
